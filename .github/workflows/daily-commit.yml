name: Daily Commit

on:
  schedule:
    - cron: '25 13 * * *'  # Fixed: Added missing space - should be '* * *' not '**'
  
  # Allow the workflow to be manually triggered through the GitHub UI
  workflow_dispatch:

# Add permissions for the GITHUB_TOKEN
permissions:
  contents: write

jobs:
  daily_commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0  # Fetch full history
        
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
        
      # Add this step before "Create daily commit"
      - name: Pull latest changes
        run: git pull origin main --rebase
        
      - name: Create daily commit
        run: |
          echo "Daily commit on $(date)" >> daily_commit.txt
          git add daily_commit.txt
          # Only commit if there are changes
          if ! git diff --staged --quiet; then
            git commit -m "Daily commit - $(date '+%Y-%m-%d %H:%M')"
          else
            echo "No changes to commit"
          fi
        
      - name: Push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push origin ${{ github.ref_name }} --force
